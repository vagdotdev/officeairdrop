{
  "name": "drop",
  "version": "0.1.0",
  "private": true,
  "description": "Office AirDrop — encrypted peer-to-peer file transfer across Mac, Windows, and Linux.",
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter @beam/server --filter @beam/client dev",
    "dev:server": "pnpm --filter @beam/server dev",
    "dev:client": "pnpm --filter @beam/client dev",
    "dev:maid": "pnpm --filter @beam/maid dev",
    "build": "pnpm --filter @beam/shared build && pnpm --filter @beam/server build && pnpm --filter @beam/maid build && pnpm --filter @beam/client build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
